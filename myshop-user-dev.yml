  spring:
    datasource:
      url:
  jdbc:mysql://192.168.138.129:3306/springcloud?characterEncoding=UTF8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true 
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: 123456

    jpa:
      show-sql: true
      generate-ddl: true
      database: mysql
      hibernate:
        ddl-auto: update

  eureka:
    client:
      register-with-eureka: true
      fetch-registry: true
      service-url:
        defaultZone: http://localhost:8888/eureka,http://localhost:9999/eureka
    instance:
      prefer-ip-address: true
      instance-id: ${spring.application.name}:${server.port}

  springdoc:
    swagger-ui:
      path: /swagger-ui.html
